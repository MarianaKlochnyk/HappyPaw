import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SupportivesrevicePage } from './supportivesrevice.page';

describe('SupportivesrevicePage', () => {
  let component: SupportivesrevicePage;
  let fixture: ComponentFixture<SupportivesrevicePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(SupportivesrevicePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
