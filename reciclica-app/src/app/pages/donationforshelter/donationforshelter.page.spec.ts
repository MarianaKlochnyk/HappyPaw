import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DonationforshelterPage } from './donationforshelter.page';

describe('DonationforshelterPage', () => {
  let component: DonationforshelterPage;
  let fixture: ComponentFixture<DonationforshelterPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DonationforshelterPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
