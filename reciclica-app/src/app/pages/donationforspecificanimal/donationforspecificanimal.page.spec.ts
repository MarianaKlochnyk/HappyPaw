import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DonationforspecificanimalPage } from './donationforspecificanimal.page';

describe('DonationforspecificanimalPage', () => {
  let component: DonationforspecificanimalPage;
  let fixture: ComponentFixture<DonationforspecificanimalPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DonationforspecificanimalPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
